<div class="genre_search bg-white p-3">
  <table class="table mx-auto  bg-white">
    <tr>
      <th class="table-warning">ジャンル検索</th>
    </tr>
      <%= form_with url: genre_search_path, method: :get do |f| %>
        <% @genres.each do |genre| %>
          <tr>
            <td><%= link_to genre.name, genre_search_path(genre_id: genre.id), method: :get %></td>
          </tr>
        <% end %>
      <% end %>
  </table>
</div>
