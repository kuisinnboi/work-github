<!DOCTYPE html>
<html>
  <head>
    <title>WorkGithub</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
   <header>
    <nav class="navbar navbar-expand-lg bg-body-tertiary" style="background-color: #e3f2fd;">
     <div class="container-fluid">
       <a class="navbar-brand" href="/">ながのCAKE</a>
       <div class="collapse navbar-collapse" id="navbarNav" >
         <ul class="navbar-nav">
          <% if customer_signed_in? %>
           <li class="nav-item">
             <%= link_to "ログアウト", destroy_customer_session_path, method: :delete, class: "nav-link" %>
           </li>
          <% elsif admin_signed_in? %>
           <li class="nav-item">
            <%= link_to "ログアウト", destroy_admin_session_path, method: :delete, class: "nav-link" %>
           </li>
          <% else %>
           <li class="nav-item">
            <%= link_to "新規登録", new_customer_registration_path, class: "nav-link" %>
           </li>
           <li class="nav-item">
             <%= link_to "ログイン", new_customer_session_path, class: "nav-link" %>
           </li>
           <% end %>
         </ul>
       </div>
     </div>
    </nav>
   </header>

    <%= yield %>
   <footer class="fixed-bottom ">
    <nav class="mt-auto navbar navbar-expand-lg bg-body-tertiary" style="height:50px; background-color: #e3f2fd;"></nav>
   </footer>
  </body>
</html>
